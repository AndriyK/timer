<h1>Edit profile</h1>

<div class="margin10">
    <%= form_for(@user) do |f| %>
        <%= render 'fields', :f => f %>
        <div class="actions">
          <%= f.submit "Update" %>
        </div>
    <% end %>
</div>