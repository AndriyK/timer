<%= form_for(@user, :html => { :class => "well"}) do |f| %>
    <fieldset>

      <%= render 'fields', :f => f %>

      <div class="form-actions">
        <%= f.submit "Sign up", :class => "btn btn-primary" %>
      </div>

    </fieldset>
<% end %>
