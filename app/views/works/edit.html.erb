<h1>Edit activity</h1>

<div class="margin10">
  <%= form_for(@work) do |f| %>
      <%= render 'fields', :f => f %>
      <div class="actions">
        <%= f.submit "Update" %>
      </div>
  <% end %>
</div>