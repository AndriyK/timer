<!--<div class="navbar navbar-fixed-top"> -->
<div class="navbar">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">Time tracker</a>
      <div class="nav-collapse">
        <ul class="nav">
          <% if signed_in? %>
              <li class="<%= active_menu?('users#show', false) %>"><%= link_to "Home", user_path(current_user) %></li>
          <% else %>
              <li class="<%= active_menu?('main#home', false)%>"><%= link_to "Home", root_path %></li>
          <% end %>
          <li class="<%= active_menu?('main#about', false)%>"><%= link_to "About", {:action => 'about', :controller => 'main'} %></li>
          <li class="<%= active_menu?('main#contact', false)%>"><%= link_to "Contact", {:action => 'contact', :controller => 'main'} %></li>
        </ul>
        <ul class="nav pull-right">
            <% if signed_in? %>
                <!--<p class="navbar-text pull-right">Logged in as <a href="#">username</a></p>-->
                <li><%= link_to "Sign out", logout_path, :method => :delete %></li>
            <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div> <!-- /navbar -->