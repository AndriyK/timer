<%= render 'shared/error_messages', :object => f.object %>

<%= f.text_area :content, :size=>"43x4" %>

<% if with_work? %>
    <%= hidden_field_tag("source[work_id]", params['work_id']) %>
<% end %>

<%= render 'shared/sources_wysiwyg_js' %>

<%= render 'shared/sources_tagit_js' %>

