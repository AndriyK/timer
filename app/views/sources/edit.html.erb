<%= form_for(@source, :html => { :class => "well"}) do |f| %>
    <fieldset>

      <legend>Please edit information</legend>

      <%= render 'fields_edit', :f => f %>

      <div class="form-actions">
        <%= f.submit "Update", :class => "btn btn-primary" %>
      </div>

    </fieldset>
<% end %>
